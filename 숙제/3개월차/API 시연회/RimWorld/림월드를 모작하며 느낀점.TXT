싱글톤의 문제점: 여기저기서 겟 인스턴스 쓰니까 삭제 이후에도 언제든지 생길 수 있다.
림월드 모작하다 느낌, 씬 전환하려고 싱글톤 타일매니저 삭제했는데
레이트 업데이트에서 자꾸 타일매니저를 불러버려서 원하지 않을 때 생성된다.

레이트 업데이트에도 destroyed시 리턴 시키는게 맞는가?

버튼을 하나의 클래스에서 관리하는 것이 맞는가?
아니면 버튼마다 여러 클래스를 만드는 것이 맞는가?

struct(관련 데이터를 그룹화해야 할 때) 
Info처럼 Struct를 많이 사용해야겠다. 초기화 편하도록
transform(위치, 크기, 회전)
Pawn(상태, 등등..)

순수 가상 클래스 계층을 여러개로 나눠야겠다.(렌더 순서 등 편하게 기본적으로 되버리게)
OBJ (GameObj, UI, 등등)
언리얼을 좀 따라할까?
GameObj(Actor, 등등)
Actor(Pawn, 등등)

COBJ에 enum OBJID(TAG?)를 넣어주자

객체 내부에 이미지를 둬서
스태틱 함수로 이미지 로드 할 수 있나?

AI를 만들 때는 State를 세분화해야 디버깅이 편하고
오류가 적다.